
const Todo = ({ todo }) => { //create Todo component with todo parameter
    return (
        <div>
            <h2>{todo.item}</h2> 
        </div>
    )
}

export default Todo //export Todo component

